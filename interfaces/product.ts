interface Product {
    id: number,
    name: string,
    description: string,
    zip: number,
    specifiers: string,
    stock: number,
    location: string,
    price: number
}

export default Product;
